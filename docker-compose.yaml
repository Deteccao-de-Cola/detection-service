services:
  api:
    container_name: detection-service
    build:
     context: .
     target: builder
    ports: 
      - '8000:8000'
    volumes:
    - ./:/src
    networks:
      - tcc-cola-network

networks:
  tcc-cola-network:
    external: true